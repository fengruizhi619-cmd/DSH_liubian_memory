# 流变系统 · 交接手册

> 生成时间：2026-09-11 23:09
> 当前负责人：梅花（skill学院 工作区）
> 项目别名：liubian / 流变系统
> 本手册面向接任者，目标是"看完即可接手运维与继续开发"。

---

## 0. 一句话说明

**流变系统 = 给 Codex 智能体用的基础设施集合体**，让每个智能体拥有"长期记忆、实时协作、技能版本管理、双通路问答"四种能力。
"流变"取义：记忆会流动、系统持续演化。

设计总纲见：`C:\Users\Feng\.codex\skills\liubian\设计\流变系统总体设计_v1.md`（v1 草案 + v2 管理员确认版 + P1/P2/P4/P5 进度追加）。

---

## 1. 四个子系统

| 子系统 | 内部名 | 职责 | 主要资产 |
|---|---|---|---|
| 流变·记忆 | liubian-memory | 日记 / 标签 / 共现 / 检索 / 用户 / 留言板 | `skills\memory-skill`、`memory.py` |
| 流变·被炉 | liubian-kotatsu | 实时聊天室 / UI / 待办 / 定时闹铃 / 回调钩子 | `memory.py` 的 kotatsu 命令族、`kotatsu_ui.py` |
| 流变·更新 | liubian-update | 技能版本 / 独立装载 / 快照广播 | `skills\auto-updater`、`tracker.py`、`tracker.json` |
| 流变·通路 | liubian-path | 双通路问答：分解检索 → 外部校验 → 汇聚 | `liubian\scripts\通路2\liubian_path2.py` |

统一入口：`skills\liubian\scripts\liubian.py`（status / path，其余透传 memory.py）。
管理面板：`skills\liubian\scripts\liubian_panel_qt.py`（PyQt5），打包版 `skills\liubian\dist\liubian_panel.exe`。

---

## 2. 目录与文件说明

### 2.1 工作区：`E:\codex_data\skill学院`

顶层 72 个条目、约 7015 个文件、约 796 MB。构成如下：

**（A）子目录**

| 目录 | 文件数 | 用途 |
|---|---|---|
| `work\` | 6120 | 日常工作的按日期记录（`YYYY-MM-DD_主题.md`）+ `diary\` 子目录；最大的历史沉淀区 |
| `铃兰\` | 510 | 成员「铃兰」的工作目录（`日记\`、`蒸馏\`、`账户\`） |
| `蔷薇\` | 151 | 成员「蔷薇」的工作目录（`work\`、`文档\`、`日记\`、`账户\`，含 deepseek-harness 相关） |
| `樱花\` | 106 | 成员「樱花」的工作目录（`技能维护\`、`日记\`、`正文\`、`设定\`、`账户\`） |
| `向日葵\` | 16 | 成员「向日葵」的工作目录（`work\`、`情节记忆系统\`、`蒸馏\`、`账户\`） |
| `米兰\` | 1 | 成员「米兰」的账户信息 |
| `memory-audit\` | 1 | 2026-07-22 跨工作区记忆审计报告 `report.md`（早期质量审计，含 5 类问题） |
| `_archive\` | 46 | 归档区。含 `desktop_codex_data_20260910\`（2026-09-11 从桌面 `C:\Users\Feng\Desktop\codex_data` 整体归档的旧工作产物） |

> `铃兰/蔷薇/樱花/向日葵/米兰` 是按"花名"划分的成员私有工作目录，各自含 `账户\`（该成员账户/KEY 备忘，属**敏感**）。

**（B）顶层散落文件（历史临时产物，约 64 个，建议后续清理）**

1. **账户/密钥类（敏感，勿外传）**：`_dump_keys.py`、`_keys_dump.txt`、`_tmp_pwd.txt`、`_tmp_users.py`、`_tmp_users.txt`、`_tmp_hash.py`
2. **编码/格式修复类**：`_enc_test.txt`、`_fix_encoding.py`、`_fix_fmt.py`、`_fix_fmt2.py`
3. **临时检查/扫描类**：`_scan19.py`、`_scan19b.py`、`_tmp_check.py`、`_tmp_check2.py`、`_tmp_search.py`、`.tmp_listen.py`
4. **临时日记/草稿**：`_temp_diary.txt`、`_tmp_diary.txt`、`_tmp_diary14.md`、`memory_temp_*.txt`（19 个）、`memory_temp_diary_茉莉*.txt`
5. **被炉监听脚本变体（已被 MCP 挂机监听取代）**：`kotatsu_listen_foreground.py`、`kotatsu_listen_long.py`、`kotatsu_listen_loop.py`、`kotatsu_listen_quiet.py`、`kotatsu_listen_quiet1.py`、`kotatsu_listen_roundtable.py`、`被炉监听日志.txt`
6. **维护/检查脚本**：`scripts_check_archive.py`、`scripts_check_archive2.py`、`scripts_check_db.py`、`scripts_check_diaries.py`、`scripts_check_rooms.py`、`scripts_cleanup_test_msg.py`、`scripts_export_gc0.py`、`scripts_inventory_gc0.py`、`scripts_sample_gc0.py`、`scripts_sample_gc0b.py`、`scripts_sample_gc0c.py`、`scripts_search_project.py`、`scripts_test_fix.py`、`scripts_test_scroll.py`、`scripts_verify_bug.py`
7. **其它**：`merge_pt.py`、`write_skill.py`、`_update_skill.py`、`Group2_Pixiv图片分析结果.md`

> 交接建议：除"敏感账户类"外基本是一次性产物，可整体归档或删除；删除前确认无引用（主流程均走 `skills\` 下正式脚本）。

### 2.2 技能目录：`C:\Users\Feng\.codex\skills\`（共 34 个技能）

**（1）`memory-skill\`（记忆系统主体，89 文件）**

| 文件 | 用途 |
|---|---|
| `SKILL.md` | 技能说明：触发条件、回答格式、核心流程、标签规则、MCP 封装、被炉、通路、对话定义 |
| `mcp\_common.py` | MCP 公共层：子进程调用 memory.py / 通路二 / 面板（`run_memory`、`run_path2`、`launch_detached`） |
| `mcp\mcp_search.py` | MCP：检索（`search_diary`、`read_diary`），全局检索 |
| `mcp\mcp_write.py` | MCP：写日记（`write_diary`、`write_log`） |
| `mcp\mcp_memory.py` | MCP：核心（`memory_info`、`list_tags`、`get_key`、`check_inbox`、`post_message`、`register_user`） |
| `mcp\mcp_panel.py` | MCP：打开管理面板 / 被炉房间 UI |
| `mcp\mcp_validate.py` | MCP：通路二校验（`validate_answer`） |
| `mcp\mcp_skill_index.py` | MCP：技能文档入语义检索（`index_skill`、`index_all_skills`、`skill_index_status`） |
| `mcp\mcp_kotatsu.py` | MCP：被炉（join/send/poll/search/todo + **挂机监听** watch_start/take/status/stop） |
| `mcp\mcp_update.py` | MCP：更新（check/list/install/uninstall/sync/snapshot/agents/subscribe/broadcast + read_skill_source） |
| `mcp\mcp_embed.py` | MCP：向量模型托管（拉起/保活 llama-server，owner 锁协调，err 日志轮转） |
| `mcp\mcp_guard.py` | MCP 防残留：清理"父进程已死"的同款孤儿 + 父进程死亡自毁 |
| `scripts\memory.py` | **记忆系统主程序**（约 90 KB）：日记/标签/检索/用户/留言板/被炉/更新/订阅/broadcast 全部子命令 |
| `scripts\memory_db.py` | SQLite 存储层（docs / diary_content / embeddings / skill_docs 四表 + 全局检索 helper） |
| `scripts\semantic_search.py` | 语义向量检索（embed_texts / index_workspace / search_semantic[_global] / 技能文档索引） |
| `scripts\kotatsu_ui.py` | 被炉系统 Web UI（实时聊天 + 待办 + 检索） |
| `scripts\memory_patch.py` / `memory_patch2.py` | 历史补丁脚本（保留，各含一份 FLOWER_NAMES 副本） |
| `scripts\patch_helper.py` / `fix_format.py` / `run_index_all.py` | 辅助脚本 |
| `scripts\memory\` | **旧 JSON 数据残留**（index.json/tags.json/cooccurrence.json/diaries/*.md），已迁移 SQLite，仅作历史备份 |
| `mcp\embed_server.err.log` / `.old` | 向量服务 stderr 日志（`.old` 为 2026-09-10 轮转出的约 42 MB 历史文件，可删） |

**（2）`liubian\`（流变系统统一技能，59 文件）**

| 文件 | 用途 |
|---|---|
| `SKILL.md` | 统一技能说明（四子系统、统一 CLI、通路二、管理面板、数据位置） |
| `scripts\liubian.py` | 统一 CLI：`status` / `path`，其余透传 memory.py |
| `scripts\liubian_panel_qt.py` | 管理面板（PyQt5，本地）：系统用户管理 / 被炉系统 / Skill 管理 |
| `scripts\liubian_panel.py` | 早期 Web 版管理面板 |
| `scripts\通路2\liubian_path2.py` | **通路二工具**：双无上下文外部 API（初始回答 + 五维度校验），输出 initial_answer / problems |
| `scripts\通路2\config.json` | 通路二 API 密钥配置（**敏感，勿外传**） |
| `scripts\通路2\usage_log.jsonl` | 通路二每次调用的用量日志（tokens/缓存/延迟） |
| `设计\流变系统总体设计_v1.md` | 项目设计总纲（v1 + v2 + 进度追加） |
| `测试\p5_round1_results.json` / `audit_report.txt` | P5 压测第 1 轮结果 / 系统审计报告 |
| `shots\` | 面板与被炉 UI 历史截图 + 分析 |
| `dist\liubian_panel.exe` | 打包后的面板（约 39 MB） |
| `build\` | PyInstaller 构建中间产物（可删） |
| `icons\` | 面板图标 |

**（3）`auto-updater\`（更新系统，7 文件）**

| 文件 | 用途 |
|---|---|
| `SKILL.md` | 更新系统说明（独立版本管理、注册/检查/同步流程、硬性规则） |
| `scripts\tracker.py` | 快照与版本核心：scan/hash/tracker.json、agent register/check/sync/status、log add/list |
| `.skill_tracker\agent_versions\*.json.dead_20260910` | 已废弃旧 JSON（2026-09-10 归档，现改用 SQLite `agent_versions:<花名>`） |

**（4）`kotatsu-always-listen\`（1 文件）**

| 文件 | 用途 |
|---|---|
| `SKILL.md` | 被炉前台/挂机监听纪律（"不结束对话"的定义、监听循环、常见陷阱自查） |

> 其余 30 个技能为写作/研究/课程/工具类，与流变系统无直接依赖，本手册不展开。

### 2.3 数据目录：`E:\codex_data\.memory_registry\`

| 条目 | 大小 | 说明 |
|---|---|---|
| `liubian.db` | 约 54.7 MB | **主数据库（SQLite）**，见下方数据字典 |
| `kotatsu_rooms\` | 约 160 MB | 被炉房间 JSON（历史消息量大；已逐步入库，此为磁盘遗留） |
| `archive\` | 约 28.8 MB | 归档数据 |
| `chat_rooms\` | 约 175 KB | 更名前的旧聊天室残留 |
| `embed_owner.lock` | 48 B | 向量模型 owner 锁（含 owner pid；**注意 pid 复用坑**，见第 5 节） |
| `kotatsu_schedule.json.lock` / `kotatsu_hooks.json.lock` | 1 B | 被炉定时/钩子配置文件锁 |

**数据字典（liubian.db）**

| 表 | 行数 | 说明 |
|---|---|---|
| `docs` | 83 | 键值文档表。key 形态：`users`、`board`（留言板）、`index:<工作区>`、`tags:<工作区>`、`cooccurrence:<工作区>`、`agent_versions:<花名>`、`current_user:<工作区>`、`kotatsu_room:<房间>`、`kotatsu_schedule`、`kotatsu_hooks` |
| `diary_content` | 6082 | 日记正文（workspace, diary_id, content, updated）。**同一 diary_id 可跨工作区重复**（如 D0001 在 13 个工作区各有） |
| `embeddings` | 5839 | 日记向量（float32 BLOB，1024 维，workspace+diary_id 主键） |
| `skill_docs` | 34 | 技能 SKILL.md 向量（全局，跨工作区） |

---

## 3. 核心机制

### 3.1 记忆系统
- **每轮流程**：写上一轮日记 → 查留言板(inbox) → 检索 → 回答；回答开头固定四行：`[日记][留言][搜索][校验]`（只写最简词汇）。
- **日记**：`memory.py write -t "标签≥5个" -s "摘要" --content "正文" -u 花名 --password 密码`；正文直传不落临时文件。
- **全局检索**（2026-09-07 改造）：搜索跨全部 13 个工作区，结果形如 `[D0001@工程0]`；读取支持 `Dxxxx@工作区`。
- **标签策略**：细小标签、禁止宽泛词、优先复用已有标签、写完自检。
- **语义检索**：tag 命中与语义余弦 1:1 融合；向量由本地 llama-server（Qwen3-Embedding-0.6B，1024 维，端口 8082）提供；不可用时自动回退纯 tag。
- **留言板**：跨对话留言，查看后自动已读；快照更新会向"订阅者"广播技能更新通知。
- **用户体系**：花名（花朵中文名，**白名单已扩至 64 个**）+ 8 位数字密码 + 每用户独立哈希 KEY；KEY 只在 shell 传，不写入回答。

### 3.2 被炉系统（KOTATSU）
- 房间：`kotatsu join/send/poll/listen/watch/serve`；消息不带名字前缀；`@管理员` 自动生成待办。
- 定时日记：每 10 条消息提示创始人记录；创始人离线自动选举新创始人。
- 挂机监听（推荐）：MCP `mcp-kotatsu` 的 `watch_start` 起后台守护线程轮询入队，`watch_take` 取消息，`watch_stop` 下线（在线状态约 70 秒后自然消失）。
- UI：`kotatsu ui --room <房间>`（管理员专用，实时+待办+检索）。

### 3.3 更新系统
- 每个智能体独立保存已装载技能的哈希；`tracker.json` 是权威快照。
- 流程：`install`（选择装载）→ `check`（比对快照）→ **强制通读变更技能 SKILL.md 原文** → `sync`（记录哈希）。
- 维护者 `snapshot` 刷新快照，自动向订阅者广播留言板通知。
- 铁则：未通读原文不得 sync。

### 3.4 通路（双通路问答）
- 通路一（主智能体）：问题分解为子问题 → 逐个用 tag 组检索记忆 → 产出结构化记忆总结。
- 通路二（外部工具）：`liubian_path2.py` 调两个**无上下文**独立 API：API-A 生成初始回答、API-B 五维度校验并列出问题（逻辑断裂/未回答问题/证据缺失/过度声称/答非所问）。
- 汇聚：主智能体结合"记忆总结 + 初始回答 + 问题清单"产出最终回答；每轮一篇日记。
- 当前定档：API-A thinking=disabled，API-B thinking=enabled(2048)。

### 3.5 MCP 封装（9 个服务器）
注册在 `C:\Users\Feng\.codex\config.toml` 的 `[mcp_servers.*]`：
`mcp-memory-search` / `mcp-memory-write` / `mcp-memory-core` / `mcp-memory-panel` / `mcp-memory-validate` / `mcp-skill-index` / `mcp-kotatsu` / `mcp-update` / `mcp-embed-model`。

- 全部为 stdio 服务器，工具命名 `mcp__<server>__<tool>`。
- 防残留：`mcp_guard.py` 启动后清理"父进程已死"的同款孤儿，并在父进程（Codex）消失时自毁。
- 向量模型：`mcp-embed-model` 负责拉起/保活 llama-server；owner 锁保证整机一份模型。

---

## 4. 项目进展（做到哪里了）

### 4.1 设计阶段（P0–P5）

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0 | 设计文档 `流变系统总体设计_v1.md`（含 v2 管理员确认版） | 完成 |
| P1 | 通路二工具 `liubian_path2.py` 落地（2026-08-12 端到端跑通） | 完成 |
| P2 | 统一 CLI `liubian.py`（status / path，其余透传 memory.py） | 完成 |
| P3 | 更名迁移 | 管理员决定**不更名**（保持 memory-skill，物理目录不动） |
| P4 | 技能文档补「流变·通路」章节 + 快照同步 | 完成 |
| P5 | 压力测试验收（每 10 段对话一轮） | 第 1 轮完成（10 类问题跑通，13983 tokens，修复 API-B 未按 JSON 输出问题） |

### 4.2 设计之外的实际演进（按时间）
- **2026-07** 记忆系统初创：日记/标签/共现/检索/多用户/哈希锁；07-22 首次跨工作区审计（5 工作区，发现"写但没写/写了不说/只写不检索"）。
- **2026-07-24~25** 缓存命中优化：固定前缀、精简输出格式（实测连续两轮零成本）。
- **2026-08-04~06** 被炉系统：聊天室 → 被炉更名、待办/@管理员、v2 服务（watch + 闹铃 + HTTP API）。
- **2026-08-12** 流变系统成型：四子系统设计、通路二、统一 CLI、管理面板（Web → PyQt5）。
- **2026-08-13~15** 工程化：记忆五功能打包为 MCP；被炉与更新并入 SQLite（纯数据库模式）。
- **2026-08-28** 技能文档入语义检索（`mcp-skill-index`）。
- **2026-08-30** 被炉挂机监听 MCP、更新系统 MCP（`mcp-kotatsu` / `mcp-update`）。
- **2026-09-07** 记忆检索**全局化**（跨 13 工作区，带 `@工作区` 标识）。
- **2026-09-08~09** MCP 防残留（`mcp_guard`）；向量服务排查（owner 锁、内存泄露实测=无泄露）。
- **2026-09-10** MCP `Transport closed` 定位并修复（`dedupe_same_parent` 自退打断连接，已回滚）；`agent_versions` 读写同源修复；花名上限扩至 64；清理桌面 `codex_data`。
- **2026-09-11** 记忆全量导出（桌面 txt，6076 篇）；撰写本手册。

### 4.3 待办
1. **MCP 工具未注入部分会话**：Codex 目前以 DeepSeek API key 直连（`auth.json` 仅 `OPENAI_API_KEY`），处于 API key 认证模式；该模式下外部 stdio MCP 工具不进模型工具面（日志持续报 `remote control requires ChatGPT authentication; API key auth is not supported`）。服务端一切正常（8 进程、握手 OK、协议层调用成功），**需 ChatGPT OAuth 登录**才能让工具注入模型。
2. **MCP 重复实例**：曾被重复拉起 3 代 × 8 个（桃花 M1306）。注意：用"同父单例自退"会打断 Codex 连接（已验证 `Transport closed`），需换方案。
3. **向量服务当前停用**：为释放显存，`mcp-embed-model` 的 `enabled=false`、llama-server 已停（显存 3764→1811 MiB），语义检索回退纯 tag。需要时改回 `true` 重启 Codex，或应急跑 `E:\llama.cpp\start_emb.cmd`。
4. **`embed_server.err.log.old`（约 42 MB）**：轮转产物，可删。
5. **工作区顶层历史临时文件**（约 64 个，见 2.1-B）：建议归档清理；账户/密钥类属敏感。
6. **P5 压力测试**：仅完成第 1 轮，计划共 10 轮。

---

## 5. 已知坑与教训（务必先读）

1. **`dedupe_same_parent` 回归（2026-09-09 引入，09-10 回滚）**：让"同一父进程下后来的同款 MCP 实例"自杀，会打断 Codex 刚建立的 stdio 连接，导致所有记忆 MCP 调用失败并报 `Transport closed`。**教训：不要用"实例自杀"做单例收敛。**
2. **向量服务 owner 锁的 pid 复用坑**：`embed_owner.lock` 记录 owner pid；若该 pid 被系统复用给别的进程（曾复用为 `svchost.exe`），`psutil.pid_exists` 会误判"owner 存活"，导致所有实例都在"等待既有实例"、没人拉起模型。已加固：owner 必须是命令行含 `mcp_embed` 的进程，否则视为陈旧可回收。
3. **`agent_versions` 读写必须同源**：早期"写 JSON、读 SQLite"导致装载技能报 `Agent 'X' not found, register first`。现已统一到 SQLite 键 `agent_versions:<花名>`；磁盘上的 `.json` 已于 2026-09-10 全部归档为 `*.dead_20260910`。
4. **历史命名残留**：`chat_*`（旧聊天）、`.memory_registry`（曾计划更名 `.liubian_registry`，决定不改）、`memory-skill`（曾计划更名 liubian，决定不改）。看到这些名字是历史，不是错误。
5. **日记 ID 非全局唯一**：`D0001` 在 13 个工作区各有一篇，跨区引用必须带 `@工作区`。
6. **删除类操作需谨慎**：删除前先确认无引用；用户在场时先说明再删。

---

## 6. 运维手册（常用操作）

### 6.1 日常（每轮对话）
```
# 写日记（标签≥5，正文直传）
python C:\Users\Feng\.codex\skills\memory-skill\scripts\memory.py write -k log -t "标签1,标签2,标签3,标签4,标签5" -s "摘要" --content "正文" -u <花名> --password <8位密码>

# 查留言板（查看后自动已读）
python ...\memory.py inbox -u <花名> --password <密码> --key <KEY>

# 全局检索（≥4 标签）
python ...\memory.py search --tags "标签1,标签2,标签3,标签4" --key <KEY> --user <花名> --password <密码>

# 读日记（跨区）
python ...\memory.py read "D0955@skill学院"
```

### 6.2 系统总览与通路
```
python C:\Users\Feng\.codex\skills\liubian\scripts\liubian.py status
python C:\Users\Feng\.codex\skills\liubian\scripts\liubian.py path "问题" --out <输出.json>
```

### 6.3 技能更新
```
python ...\memory.py update -u <花名> --password <密码> check       # 检查
python ...\memory.py update -u <花名> --password <密码> install --skill <技能名|all>
python ...\memory.py update -u <花名> --password <密码> sync        # 通读后同步
python ...\memory.py update -u <花名> --password <密码> snapshot    # 维护者刷新快照（会广播）
python ...\memory.py update -u <花名> --password <密码> agents      # 全局安装状态
python ...\memory.py broadcast -u <花名> --password <密码> --content "公告"
```

### 6.4 管理面板
```
python C:\Users\Feng\.codex\skills\liubian\scripts\liubian_panel_qt.py
# 或直接运行 dist\liubian_panel.exe
```

### 6.5 故障处置

**（a）记忆 MCP 全部不可用（调用报 `Transport closed`）**
1. 查进程：`Get-CimInstance Win32_Process -Filter "Name='python.exe'"` 并筛命令行含 `memory-skill\mcp`；
2. 确认是否重复实例、是否都挂在当前 `codex.exe` 下；
3. 清残留后**重启 Codex**（MCP 连接在会话初始化时建立，运行中的会话不会自动重连）。

**（b）向量服务掉线（语义检索回退纯 tag）**
1. 查端口：`Get-NetTCPConnection -LocalPort 8082 -State Listen`
2. 查锁：`Get-Content E:\codex_data\.memory_registry\embed_owner.lock`（确认 pid 是否真是 `mcp_embed` 进程）
3. 应急拉起：`E:\llama.cpp\start_emb.cmd`
4. 若锁被无关进程占用（pid 复用），删除该锁后重启 Codex。

**（c）技能装载报 `Agent 'X' not found`**
说明 `agent_versions` 读写不同源。确认 `memory.py._ensure_agent_record` 与 `tracker.py` 都走 SQLite 键 `agent_versions:<花名>`。

**（d）MCP 数量暴涨**
`mcp_guard` 只清"父进程已死"的孤儿；若同一 Codex 反复重新拉起，会出现多代实例。勿用"自退"方案收敛（见第 5 节），改为排查 Codex 为何重复 spawn。

---

## 7. 成本与缓存要点
- 主智能体走 DeepSeek，缓存命中直接决定成本；**保持固定前缀**（技能文本 + 记忆流程格式）是命中率关键。
- 记忆流程输出格式（四行标记）刻意极简，就是为了前缀稳定。
- 通路二每轮固定 2 次外部调用，均为无上下文一次性请求，用量写入 `usage_log.jsonl`。
- 向量服务常驻占显存约 1.9 GB，不需要时可停用（见 4.3-3）。

---

## 8. 关键路径速查
```
主程序         C:\Users\Feng\.codex\skills\memory-skill\scripts\memory.py
存储层         C:\Users\Feng\.codex\skills\memory-skill\scripts\memory_db.py
语义检索       C:\Users\Feng\.codex\skills\memory-skill\scripts\semantic_search.py
被炉 UI        C:\Users\Feng\.codex\skills\memory-skill\scripts\kotatsu_ui.py
统一 CLI       C:\Users\Feng\.codex\skills\liubian\scripts\liubian.py
通路二         C:\Users\Feng\.codex\skills\liubian\scripts\通路2\liubian_path2.py
管理面板       C:\Users\Feng\.codex\skills\liubian\scripts\liubian_panel_qt.py
更新核心       C:\Users\Feng\.codex\skills\auto-updater\scripts\tracker.py
MCP 目录       C:\Users\Feng\.codex\skills\memory-skill\mcp\
技能快照       C:\Users\Feng\.codex\skills\.skill_tracker\
主数据库       E:\codex_data\.memory_registry\liubian.db
被炉房间       E:\codex_data\.memory_registry\kotatsu_rooms\
设计文档       C:\Users\Feng\.codex\skills\liubian\设计\流变系统总体设计_v1.md
全局配置       C:\Users\Feng\.codex\config.toml（[mcp_servers.*] 在此）
```

---

## 9. 交接清单
- [ ] 确认接手者花名已在记忆系统注册（花名 + 8 位密码），并完成一次 `write`（取得 KEY）。
- [ ] 运行 `liubian.py status` 与 `memory.py info`，核对用户数/日记数/房间数。
- [ ] 执行一次完整记忆流程（写日记 + inbox + 检索）确认链路。
- [ ] 了解 4.3 待办（尤其 MCP 注入与向量服务停用状态）。
- [ ] 通读第 5 节"已知坑"。
- [ ] 处理工作区顶层历史临时文件（可选，注意敏感文件）。

（手册结束）
