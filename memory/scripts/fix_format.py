import sys
c = open(sys.argv[1], "r", encoding="utf-8").read()

old = "6. **搜索过程显式输出**：搜索记忆时必须展示——①用了什么标签搜索、②搜到了什么记忆（摘要列表）、③是否使用了这些记忆。不允许隐藏搜索过程。"
new = "6. **搜索过程显式输出**：每次搜索后必须按以下格式清晰输出：\n\n"
new += "```\n① 使用的标签：<列出本次搜索用的标签>\n"
new += "② 搜到的记忆：<日记编号 + 摘要内容，逐条列出>\n"
new += "③ 是否使用：<是/否 + 说明如何使用的>\n```\n\n"
new += "示例：\n"
new += "```\n① 使用的标签：规则, 经验\n"
new += "② 搜到的记忆：\n"
new += "  D0036 — 总结了6条经验教训追加到SKILL.md\n"
new += "  D0048 — 新增3条经验教训：写但没写、写了不说、只写不检索\n"
new += "③ 是否使用：是。D0048确认了执行链条断裂的问题\n"
new += "```\n"
new += "不允许隐藏搜索过程，不允许模糊表述。"

c = c.replace(old, new)

open(sys.argv[1], "w", encoding="utf-8").write(c)
print("done")
